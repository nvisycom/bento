# Build from the repo root:  bentoml build -f packages/elide-bento-ocr/bentofile.yaml .
# Image config (Python version, system packages, requirements) lives in
# service.py; this file selects the service and scopes the bundled source.
service: "elide_bento_ocr.service:OcrService"
include:
  - "packages/elide-bento-ocr/**"
  - "packages/elide-bento-core/**"
  - "pyproject.toml"
  - "uv.lock"
